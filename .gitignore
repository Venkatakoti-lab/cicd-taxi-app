# Terraform generated files and directories
.terraform/
.terraform.lock.hcl # While often included, some prefer to ignore it if dependencies are managed differently
*.tfstate
*.tfstate.*.backup
*.tfvars
*.tfvars.json
*.plan
.vscode/ # If using VS Code, ignore workspace settings and temporary files